<div class="panel-body">
  <div class="row">
    <div class="container">
      <%= simple_form_for(@club) do |f| %>
          <%= f.input :name, label: "Başlık:", placeholder: "Lütfen başlık giriniz!" %>

          <%= f.button :submit, "Gönder", :class => "btn btn-primary" %>
      <% end %>
    </div>
  </div>
  <hr \>
  <%= link_to "Geri", root_path, :class => "btn btn-sm btn-default"%>
</div>
